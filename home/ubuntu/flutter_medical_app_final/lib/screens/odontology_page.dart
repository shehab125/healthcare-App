import 'package:flutter/material.dart';

class OdontologyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Odontology')),
      body: Center(child: Text('Odontology Page')),
    );
  }
} 