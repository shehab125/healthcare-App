import 'package:flutter/material.dart';

class OncologyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Oncology')),
      body: Center(child: Text('Oncology Page')),
    );
  }
} 