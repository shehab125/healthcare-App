import 'package:flutter/material.dart';

class GynecologyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gynecology')),
      body: Center(child: Text('Gynecology Page')),
    );
  }
} 