import 'package:flutter/material.dart';

class PharmacyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pharmacy')),
      body: Center(child: Text('Pharmacy Page')),
    );
  }
} 